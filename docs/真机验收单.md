# 真机验收单（电视边上看这一页就够）

眼下所有代码层面的活都收口了，**只剩这一步需要人**。大约 8 分钟：前 4 步验线路，第 5、6 步验节目单。
不用写字，在方框里打勾、把不像的台名记住就行。回来照最后那节说三四句话，我就能决定下一段做什么。

> 这一页里有些数字后面挂着 `〔数:aptv:suspect〕` 这种东西，那是给我看的记号（意思是「这个数
> 从全量表的第一线可疑台数来的，一条命令就能重算：`./.venv/bin/python -X utf8 scripts/doc_num.py`」）。
> **读的时候直接跳过**，它不是要你操作什么。

---

## 第 0 步：先决定验哪一版表（30 秒，这步决定第 5、6 步能不能做）

电脑上的局域网服务要在跑（没跑就双击 `启动电视订阅服务.command`）。
**订阅地址照终端窗口里打印的那一行填**，别抄任何文档里写死过的 IP —— 路由器给这台 Mac 的地址会换。

> 那个窗口要是已经开了一整天以上，**先关掉再重新双击一次**。2026-09-22 抓到过一次：
> 手机打开那一页，它写的台数还是「起服务那一刻」抄下来的旧数，而磁盘上那张表早就换过版了
> （改动写在计划书 2.29）。新代码每次都现读文件，但**你手上那个进程是新的还是旧的，
> 只有重开一次才知道** —— 这一页是给我们自己看的，别让它把我们带偏。

> 不用猜，有一条命令能问：在电脑上跑 `./.venv/bin/python -X utf8 scripts/selfcheck.py`，只看 `page` 那一行。
> ✓ = 那一页说的数就是磁盘上现在这张表，往下走；写着「跑着的是**旧版**」= 照上面那段重开一次；
> 说「端口 8787 上没有订阅服务」= 还没起，双击一下再跑。它顺带把项目里另外几条对账一起跑了
> （计划书 2.30），**不碰外网、一个文件都不写**，跑一遍约五秒。

| 版本 | 电视上那张表长什么样 | 能验什么 |
|---|---|---|
| **A. 什么都不动**（现在这张，9 月 21 日 21:18 那轮实测表） | 98〔数:aptv:channels〕 个台 / 226〔数:aptv:lines〕 条线路：其中 **167**〔数:aptv:public-passed〕 条公网线路每一条都实测通过，另外 **59**〔数:aptv:intranet-lines〕 条是运营商内网那一档，电脑上根本没测过。头部写的节目单地址是上游那条，**中午又发了一次请求，仍然是 404** | 只能验第 1~4 步（线路）。第 5 步一定看不到节目条，那是预期的，不是坏了 |
| **B. 让我先重出一次**（你回一句「跑 replay」即可） | 同一批线路、同一批判决，一条不少也不多；只多两样：头部换成 `e.erw.cc`，**69 个台的 `tvg-id` 换成数字** | 1~6 步全能验。这一版才是 P3 做完之后的样子 |

> B 那一步我为什么不自作主张：它改的是电视正在读的那张表，而那 69 个 `tvg-id` 可能挂着 APTV 的收藏（第 6 步就是量这件事）。

---

## 1. 点湖南卫视（在 `📍 湖南本地` 那一组）

它的第一线是湖南广电自己的公网流（`hlsal-ldvt.qing.mgtv.com`，家里电脑实测 298ms 通过），
不是上次那个会超时的地址。这一步是整个项目最想要的答案：**电脑测出来能通的那条，电视点得开吗。**

- [ ] **能动、有画有声** → 「按实测延迟排第一线」这套排序在家里是有效的，往后照这条走。
- [ ] 黑屏 / 一直转圈 → 说明「电脑连得上」不等于「电视撑得住」，我要把公网再拆一档（国内官方 / 国内 CDN / 海外中转）重排。

## 2. 点湘潭新闻综合（`📍 市州台`）

手工挖进来的第一个台。它有两线：第一线是湘潭广电的官方流（`live.hnxttv.com:9601`），
第二线是湖南移动的 IPTV 内网地址（在 Wi-Fi 上基本等于没有）。官方流这条电脑侧验过分片在滚动，
但**两轮实测延迟差得很大**（13:00 是 40ms，21:18 是 462ms）。

- [ ] 能动，可能稍慢 → 「手工挖官方流」这条路可以照着复制到别的市州台。
- [ ] 一直卡 / 播几秒停 → 462ms 在电视上就是不行，那我按延迟重排，不再把慢的官方流排第一。
- [ ] 完全打不开 → 电脑侧它是通的（200、462ms），电视上却打不开，那要么这个流认 IP/UA，
      要么它只对联通内网放行 —— 这条台就退回「只有机顶盒能看」那一类。

## 3. 点三个被报告点名的台：湖南经视、长沙新闻综合、株洲新闻综合

它们**一条公网线路都没有**（39〔数:aptv:no-public-channels〕 个这种台里的三个）。注意这一步在验一件别的事：
**这 59〔数:aptv:intranet-lines〕 条内网线路电脑上从来没测过**（那两个源标了 `probe: false`），所以它们在 Wi-Fi 上通不通，
只有你这台电视能回答。

现在这两个台各自的第一线是谁、值多少分，是工具算出来的（report.md 的「第一线主机集中度」，计划书 2.23）：
湖南表 39〔数:hunan:channels〕 个台里 **37 个**〔数:hunan:suspect〕的第一线落在同一对专网出口上 ——
`tvgslb.hn.chinamobile.com:8089` 20〔数:hunan:focus:tvgslb.hn.chinamobile.com〕 个台、
`58.20.64.92:9999` 17〔数:hunan:focus:58.20.64.92〕 个台。所以这一步点两下就够：

- [ ] **超时 / 转圈不出画** → 「内网线路在家庭 Wi-Fi 上不可用」这个推断就是被证实了的，
      市州台的缺口要去别处找（机顶盒 / 专网 VLAN），不用再翻公网候选表了。
      **这多半就是结果**：改排序之前那轮，这 39 个台的第一线全是专网地址，真机上是整张表全超时
      （计划书 2.8），那两个端口至今只有那一次判分。
- [ ] **竟然能动** → 大事，比 2.22 那个「17 个台」的估算还大：它一次把那两家专网出口挂着的
      全部第一线（`tvgslb` 22〔数:aptv:focus:tvgslb.hn.chinamobile.com〕 个台
      + `58.20.64.92` 17〔数:aptv:focus:58.20.64.92〕 个台）从「只有机顶盒能看」里捞回来。
      哪一家动了、动的是第几条线，都请记下来 —— 解法和优先级要按这个重写。

**但请不要顺手去点这些台的第 2、第 3 条线路** —— 这件事 2.25 已经算完了（计划书 2.25，纯离线量的表结构）：
第一线可疑的 39〔数:aptv:suspect〕 个台里：**24 个**〔数:aptv:no-alt〕一条备选线路都没有、
**1 个**〔数:aptv:same-host〕的备选还在同一家机房、**14 台**〔数:aptv:other-intranet〕
从 `58.20.64.92`（湖南联通 IPTV 出口）换到 `tvgslb`（湖南移动 IPTV 门户）—— 两家都是专网。
**一个台都换不到公网电视线路**（湖南表 39〔数:hunan:channels〕 个台里的例外只有两个：湖南卫视走湖南广电官方公网流，
湘潭新闻综合走台方官方流 —— 就是第 1、2 步那两个）。把表加宽到每台 6 条线路再算一遍，
多出来那一条是蜻蜓 FM 电台（有声音、没画面），换得到的还是 0 个 —— 但**那句说的是「加宽到 6 条」那张表**，
不是电视上这份，所以它不挂记号（`doc_num.py` 只认 `data/output/` 里躺着的那两张）。
所以这一步的价值全在「第一线动没动」那一个问题上。
具体哪 17 个台挂在 `58.20.64.92`、哪 22 个挂在 `tvgslb`，看 `report.md` 的「第一线主机集中度」
那一节 —— 上面那串「24 个没有备选 / 14 个换过去仍是专网 / 0 个换得到公网」**现在也是它自己算的**，
不用跑脚本（计划书 2.26；两张表各一句：全量 39〔数:aptv:suspect〕 个可疑 → 24/1/14/**0**〔数:aptv:other-public〕，
湖南表 37〔数:hunan:suspect〕 个可疑 → 22/1/14/**0**〔数:hunan:other-public〕。
**这两句末尾那个 0 才是结论**：换得到公网电视线路的台数是 0 ——
那串「24/1/14」不挂记号，四个数各挂一个会把这行读成表格，而它只在这张表换版时才需要重看）。
眼下躺着的那份 `report.md` 是 21:18 生成的，还没有这一节 —— 跑一次 `build --replay` 就有（计划书 2.23）。

> 顺带：第 4 步那张试播包里的 **① 和 ② 就是这两个主机族各一条第一线**
> （① 湖南移动 IPTV 门户 `tvgslb:8089`，代表台是湖南国际；② 湖南联通 IPTV 出口 `58.20.64.92:9999`，
> 代表台就是湖南经视）。所以这一步点它们两条，等于同时把那 39 个台的第一线验了。

## 4. 扫一遍试播包 `probe-pack.m3u`（17〔数:pack:items〕 条，每条是一个主机族的第一线代表）

这份包 **2026-09-22 16:43 重新生成过**（计划书 2.24）：改成了按「一个主机族挂着几个台的第一线」排序，
所以条数从 16 变 17、**编号全部变了**。电视里如果还订着旧的那份，先重新订阅一次再走这一步。
报结果的时候说**主机族名字**，别说编号 —— 编号是排出来的，换一批上游就会重排。

这 17〔数:pack:items〕 条覆盖 98〔数:aptv:channels〕 个台里 **94**〔数:pack:cover〕 个台的第一线（一条不落要 `--top 0`，那是 26 条），
所以「哪几条能动」这一句，比逐个台点更能告诉我该留谁、该划谁。

| 编号 | 主机族 | 挂着几个台的第一线 | 电脑实测 |
|---|---|---|---|
| ① | 湖南移动IPTV门户 `tvgslb:8089` | **22**〔数:aptv:focus:tvgslb.hn.chinamobile.com〕（第 3 步那 20 个「换线路也换不出去」的台在里面） | 从没测过 |
| ② | 湖南联通IPTV出口 `58.20.64.92:9999` | **17**〔数:aptv:focus:58.20.64.92〕（湖南经视） | 从没测过 |
| ③ | 阿里云公网(国内) `120.76.248.139` | 15 | 17/17 通·48ms |
| ④⑤⑥ | 美国 Nocix 中转 `107.150.60.122` / `63.141.230.178:82` / `74.91.26.218:82` | 11 / 5 / 5 | 40/40·911ms、44/44·1496ms、15/15·959ms |
| ⑦ | 美国 WholeSale 中转 `204.12.221.218:8181` | 5 | 10/12 通·957ms |
| ⑧ | `bztv.tvbus.cc:8081` | 3 | 4/4 通·585ms |
| ⑨ | `207.56.13.146:81` | 2 | 5/5 通·541ms |
| ⑩ | `112.27.5.218:9901`（江西卫视、西藏卫视） | 2 | 2/2 通·64ms |
| ⑪ | `112.30.73.119:229/9901`（CCTV-3、CCTV-5 各一条） | 2 | 2/2 通·54ms |
| ⑫⑬ | 美国中转 `198.204.228.26` / `192.151.150.154` | 1 / 1 | 13/19 通·953ms、5/5 通·963ms |
| ⑭ | `38.64.72.148:80`（福建海峡卫视） | 1 | 4/4 通·431ms |
| ⑮ | **★湖南广电官方公网**（湖南卫视） | 1 | 1/1 通·298ms |
| ⑯ | 264788 中转（宁夏卫视）**对照组** | 0，它没挂任何台的第一线 | 16/16 通·2969ms |
| ⑰ | **★湘潭广电官方公网**（湘潭新闻综合） | 1 | 1/1 通·462ms |

三条要先记住：

- **⑮ 和 ⑰ 是第 1、2 步那两个台的官方流**，★ 表示基准：它们要是不动，第 1、2 步的结论就作废。
- **⑯ 名字里写着「实测是整段 MP4 录像」**，它验的不是能不能播，是**我判得准不准**：
  播起来像直播 → 我那条内容判据看走眼了，单独告诉我。（它同时也是全表最慢的一条：2969ms。）
- **④⑤⑥⑦⑫⑬ 这六条都是美国中转族**，它们能动但卡是最常见的情况 —— 卡不卡正是要记的。

- [ ] 记下**能动**的是哪几条（说主机族名字），以及哪些「能动但一直卡」。
- [ ] 顺便留意有没有「点开是录像不是直播」的（画面在动但像播旧节目、进度条能拖）—— 除了 ⑯，
      其余都被我判成直播，那就是判据没认出来的漏网。
- [ ] ① 和 ② 是全表最重手的两条（上面那 39〔数:aptv:suspect〕 个可疑台，第一线全在这两家上），
      而且**电脑从来没测过它们**
      （那两个源标了 `probe: false`）：如果遥控器只够点两条，点这两条。

---

## 5. 节目条验不验得到（**只有 B 版表能验**）

点这四个，前三个都该有节目条，第四个是**故意留的对照组**：

| 台 | 2026-09-23 那份节目单里几条（09-22 那份数出来前三个一字不差） | 该看到什么 |
|---|---|---|
| 湖南卫视 | 36 | 有节目条，且节目名对得上现在播的 |
| CCTV-13新闻（`📺 央视`。**只有全量表 `aptv.m3u` 里有这个台**，只订了 `hunan.m3u` 就跳这一行） | 36 | 有节目条 |
| 湖南经视 | 20 | **有节目条，但播不出来** —— 它节目单里有、公网线路没有。看到「有节目条 + 黑屏」正好说明这两件事是两条独立的账 |
| 湘潭新闻综合 | 0 | **不该有节目条**（那份节目单里根本没有湘潭这个键）。它要是冒出节目条，说明 APTV 是拿台名自己去猜的 —— 不一定是坏事，但我这一层的对账口径得重看 |

- [ ] 前三个出、第四个不出 → 节目单这条链在家里通了，收口。

（上面那一列是**按天**数的：同一个台隔天就变 —— 湖南卫视 09-21 记的是 37 条，09-22 那份缓存里
现数是 36 条；金鹰纪实 09-22 是 43 条、09-23 这份里只剩 38 条。所以那几个数只给人一个
「大概该有几十条」的预期，**不是判据**，判据是「有没有」。想自己现数：
`./.venv/bin/python scripts/epg_check.py data/cache/epg.xml`。
这几个数是 2026-09-23 上午现重抓的那份节目单里数出来的（09-22 那份里前三个台一字不差，
对照组「湘潭」在两份里都还是 0 个频道块 —— 它是真不存在，不是我数错了）。
中间发生过一件事：09-23 凌晨那份缓存只覆盖 09-20~09-22，`epg_check` 那时会回一句
「没有今天的内容，距今 1 天」并**退 1** —— 那是尺子在说真话，不是项目坏了；
上面那次重抓就是把这一天补上了。）
- [ ] 只有 CCTV-13 出、湖南那几个不出 → 先别当我配错了：看一眼 APTV 订阅详情里的节目单地址
      是不是已经变成 `e.erw.cc`（还是那条 404 就说明你看的仍是 A 版表）。
- [ ] 全都不出 → 大概率是家里那张 Wi-Fi 取不到 `e.erw.cc`（这台电脑能取到，但它走的是代理隧道，不算家里）。
      下一步是**换 `config/epg.yaml` 里的 `backup_url`**（112114，也是境内服务）再验一次 ——
      但要注意它也是一次换表：那 69 个台的 id 会再抖一遍。
      **别为了这个去动代理**，那会让整台机器上别的软件一起断。

## 6. 换 id 会不会把收藏弄掉（B 版表生效**之前**先做）

- [ ] 先在 APTV 里收藏两个台：一个会被改 id 的（湖南卫视 → `81`），一个不会被改的（随便某个市州台，它本来就没配上节目单）。
- [ ] 新表生效之后回来看：收藏还在不在？

这一条的答案会决定要不要加一条规则：如果收藏掉了，说明 APTV 拿 `tvg-id` 当频道主键，
那就给这一层加「id 只在第一次生效、之后按台名钉住」；如果没掉，就不加那条 ——
钉住之后再换任何一份节目单都配不上了（这个反面代价是 09-22 量退路时才看清的）。
我不猜，等你这一勾。

> 一颗刚量出来的定心丸（09-22 晚，计划书 2.26）：**换成 B 版表不动任何一条线路。**
> 我离线重出了一遍（`build --replay`，只沿用 21:18 那份判决，**线路**一条都没测、产物目录没动），
> `aptv.m3u` 的 226 条、`hunan.m3u` 的 58 条地址和顺序与电视上这份**逐字节相同**，
> 差异全在 `tvg-id` 和头部那行节目单地址上。所以这一步验的确实只是「换 id 的代价」，不是「换线路的代价」。
>
> 09-23 上午又走了一遍同样的命令，这一次**节目单换成了当天那份**（`e.erw.cc` 重抓，
> 519 个频道变 518、18 147 条节目变 18 078）：226 / 58 条线路仍然一字不差
> （`./.venv/bin/python -X utf8 scripts/table_drift.py --against /tmp/r232` 退 0）。也就是说「不动线路」这一句连着两份
> 不同的节目单都成立 —— 它只说线路，收藏那一问（第 6 步）还是要你在电视上看一眼才有答案。

---

## 回来只要告诉我这几句

1. 第 1、2 步动不动、第 3 步是不是超时；
2. 第 4 步能动的编号（以及哪些「能动但一直卡」）；
3. 第 5 步有没有节目条、第 6 步收藏在不在；
4. 要不要我把 `data/output/report.md` 附给你看（它是那张表的底账，只读，改不动电视上的东西）。

如果哪一步和预期不一样，把 APTV 里那个台的报错或截图留着，**先别重启、也别重跑生成** ——
这台机器上现在的证据（`data/output/` 那批产物）是 21:18 那一轮的，一次误跑就盖没了。
