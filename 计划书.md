# 湖南本地 IPTV 源聚合工具 — 计划书

> 目标平台：APTV（Apple TV / iPhone / Mac）
> 核心诉求：在 Apple TV 上稳定观看湖南本地频道（省台 + 长沙市台 + 14 市州台），附带央视与主流卫视
> 运行环境：开发机 Windows（可开代理）　｜　生产部署 家中飞牛 fnOS NAS（Docker）
> 文档日期：2026-09-18　｜　版本 v1.2（决策全部确认 + NAS 环境已实测核对）　｜　状态：**P0 开工中**

---

## 一、目标与非目标

### 1.1 要做的

做一个**跑在飞牛 NAS（Docker 容器）里的命令行工具 + 定时任务**，开发调试在你的 Windows 上进行，自动完成四件事：

1. **搜源**：从多个公开维护的 IPTV 聚合仓库抓取频道列表
2. **匹配**：把杂乱的频道名归一化，识别出"这一条是湖南经视"，并合并同一频道的多条线路
3. **验源**：并发实测每条线路是否真能播、延迟多少，剔除失效源，为每个频道保留最优的 N 条
4. **出源**：生成 APTV 可直接订阅的 `.m3u`（含分组、台标、节目单 EPG），由 NAS 上的轻量 HTTP 服务提供订阅地址；同时保留本地文件副本

最终你在 APTV 里只需填一个局域网订阅地址（形如 `http://192.168.1.3:8787/aptv.m3u`），之后每天自动更新，打开就能看湖南卫视、湖南经视、长沙新闻综合……既不用管源失效，也不用担心电脑没开机。

### 1.2 不做的（本期范围外）

| 项目 | 原因 |
|---|---|
| 运营商组播 / 局域网 IP 扫描 | 你已选择"仅聚合公开源"。组播源只在家庭内网可用、且可能违反运营商服务条款 |
| 图形界面 App | 命令行 + 配置文件足够，GUI 会显著拉长工期。如后期需要可加 |
| 录制、时移、回看 | APTV 自身能力范围，不由本工具负责 |
| 破解付费源、盗链需登录的私有流 | 合规红线，见第十一章 |
| 海外 / 港澳台频道 | 你选择的范围是"湖南本地为主 + 央视/卫视" |

---

## 二、调研结论（2026-09-18 实测，非推测）

开工前我对候选公开源做了真实探测，结论如下。**这一节决定了后面的技术路线，请重点看。**

### 2.1 候选上游源可用性实测

| 上游源 | 探测结果 | 湖南频道命中 | 结论 |
|---|---|---|---|
| `raw.githubusercontent.com/Guovin/iptv-api/gd/output/result.m3u` | **200**，396 KB，**1619 条频道**，http 852 / https 764 / rtmp 3 | **35 条**，含独立分组「☘️湖南频道」 | ✅ **主力源** |
| 同上仓库 `master` 分支同路径 | **404 Not Found** | — | ⚠️ 分支路径会变，不能硬编码 |
| `raw.githubusercontent.com/fanmingming/live/main/tv/m3u/index.m3u` | 200，94 条频道 | 仅 2 条 | 🔸 备用源（湖南覆盖弱） |
| `raw.githubusercontent.com/fanmingming/live/main/tv/m3u/itv.m3u` | 200，189 条频道 | 3 条 | 🔸 备用源 |
| `live.fanmingming.com/tv/m3u/global.m3u` | **000 连接超时** | — | ❌ 国内直连不可用，需镜像回退 |
| `iptv-org.github.io/iptv/countries/cn.m3u` | 200，144 条频道 | 0 条 | 🔸 仅作央视/卫视兜底 |

**要点**：`iptv-api` 的 `gd` 分支是目前对央视/卫视覆盖最好的公开源。但它对湖南市州台几乎为零 —— 那部分要靠运营商口径的湖南源补，见 2.8。无论哪种，架构上都必须做**多上游冗余 + 镜像回退**，不能依赖单一 URL。

> 🔧 2.1 这张表只覆盖了通用聚合源。后来找到的 `hn_mobile` / `hn_unicom` / `hn_telecom` 三个湖南专项源见 2.8 与 `config/sources.yaml`。

### 2.2 湖南频道实际覆盖情况（来自 iptv-api `gd` 分支）

| 频道 | 可用线路数 | 频道 | 可用线路数 |
|---|---|---|---|
| 湖南卫视 | 5 | 湖南电影 | 2 |
| 湖南经视 | 5 | 湖南娱乐 | 2 |
| 湖南都市 | 5 | 湖南国际 | 2 |
| 湖南电视剧 | 5 | 湖南教育 | 1 |
| 金鹰卡通 | 5 | 湖南爱晚 | 1 |
| 金鹰纪实 | 5 | 长沙新闻综合 | 1 |
| 湖南公共 | 4 | **14 市州台** | **0 ～ 1** |

**这是本项目最大的风险点**：省级频道和金鹰系覆盖良好，但**株洲、湘潭、岳阳、衡阳、常德、张家界、益阳、郴州、永州、怀化、邵阳、湘西的市州台在公开聚合源里几乎完全缺失**（实测关键词命中数全为 0，仅娄底 1 条）。所以"湖南地市台补源"必须作为一个独立工作项（第 8 章 P5），靠公开仓库是拿不到的。

> 🔧 **2026-09-18 晚修正：这个结论只对「通用聚合源」成立。** 换用运营商口径的湖南源后，13 个市州的新闻综合台**全部拿到线路**。详见 2.8。

### 2.3 线路可用性抽样实测

从湖南频道里随机抽 6 条线路做真实请求：

- **5 条**返回 `HTTP 200` + 合法的 `#EXTM3U` 播放列表，首字节延迟 0.03 ～ 1.97 秒
- **1 条**（`stream1.freetv.fun`）12 秒超时，完全不可用

**要点**：公开源的失效率约 1/6 甚至更高，"能拿到 URL"和"能看"是两回事。所以**质量检测不是可选项，是必做项**。另外注意：返回 200 且是合法 m3u8 **仍不代表能播**（可能是空列表、或需要 token 的子播放列表），检测必须下探到分片层（见第 6 章）。

### 2.4 EPG（节目单）

- `epg.51zmt.top:8000/e.xml`：直接请求返回 **302 跳转**，跟随重定向后 `200 application/xml` — 可用，但客户端必须开启重定向跟随
- `iptv-api` 输出的 m3u 头部自带 `x-tvg-url`，指向其自建 `epg.gz`（经 `gh-proxy.com` 加速）— 可作首选，因为它的 `tvg-id` 与频道天然对齐
- **待实施期验证**：EPG 中湖南地方台的 `channel id` 是否与我方 `tvg-id` 一致。若不一致，节目单会显示空白，需要在 `enrich` 模块做 id 映射表

### 2.5 本机与家庭环境

| 组件 | 版本 / 状态 | 说明 |
|---|---|---|
| Python | **3.12.10**（另有 3.14.6） | ✅ 满足要求，选用 3.12 |
| Node.js | v24.16.0 / npm 11.13.0 | 可用，但本项目不需要 |
| Git | 2.55.0.windows.2 | ✅ 发布到托管需要 |
| ffmpeg | **未安装** | ⚠️ 影响"真实码率/分辨率"检测，见第 6.4 节 |
| 开发机 | Windows + Git Bash | 用于开发与手动运行 |
| **代理** | **可用**（2026-09-18 补充） | ✅ 关键改善：GitHub raw / 镜像站 / 海外源可直连 |
| **家庭 NAS** | **飞牛 fnOS @ 192.168.1.3**（已登录只读核对） | ✅ 详见 2.7 环境核对结果 |
| 开发机 IP | `192.168.1.4 / 24`，网关 `192.168.1.1` | ✅ 与 NAS **同网段**，可直连 |

### 2.6 补充环境对方案的影响（2026-09-18 更新）

你补充的"有代理 + 有飞牛 NAS"把方案从"能用"推向"好用"，具体影响如下。

**① 代理可用 → 抓取链路大幅简化**

2.1 节实测到的 `live.fanmingming.com` 直连超时、`raw.githubusercontent.com` 偶发不稳，在代理下都不再是问题。因此镜像回退链（`gh-proxy.com` / `ghproxy.net`）**从"必需"降级为"兜底"**，代码里保留但不再走主路径。同时更多海外源（iptv-org 全球库、部分台站官方流）变为可用，P5 补源的可选面更宽。

**② 飞牛 NAS → 部署形态升级（已成为推荐方案）**

NAS 是常开机器且 fnOS 提供图形化 Docker 管理，这让它能一次性承担三个角色：

| 角色 | 说明 | 好处 |
|---|---|---|
| **定时更新执行者** | 容器内 cron 每天自动跑「抓源 → 检测 → 生成」 | 不依赖你的 Windows 电脑开机 |
| **订阅网址托管者** | 容器内置轻量 HTTP 服务，Apple TV 直接订阅 `http://<NAS局域网IP>:<端口>/aptv.m3u` | **列表不公开暴露**；局域网取列表几乎零延迟 |
| **补源管理界面**（可选） | 一个小网页，你在手机/电脑上粘贴新找到的市州台 URL 即可入库 | 解决 P5 手工补源的便利性问题 |

对比原方案的 GitHub Pages：NAS 方案的播放列表**不会公开到互联网**，且 Apple TV 与 NAS 同处局域网、加载极快。代价是**外出时默认访问不到** —— 如果需要在外面也能看，加一层 Tailscale（fnOS 上有成熟方案）或 frp 穿透即可，属于可选项，不必一开始就做。

> ⚠️ **待真机验证**：tvOS 对访问局域网 HTTP 地址可能存在「本地网络」权限提示，APTV 首次添加局域网订阅时是否需要在 Apple TV 上额外授权，需在 P4 阶段实测确认。这不影响方案可行性，只影响首次配置的步骤说明。

### 2.7 NAS 环境核对结果（2026-09-18 已登录只读检查）

用你提供的账号登录 `http://192.168.1.3:5666` 核对，**未做任何修改、未安装任何东西**：

| 检查项 | 实测结果 | 对方案的影响 |
|---|---|---|
| NAS 局域网地址 | **192.168.1.3**（fnOS Web 端口 5666） | 订阅地址确定为 `http://192.168.1.3:8787/...` |
| 与开发机网段 | 开发机 `192.168.1.4/24`，网关 `192.168.1.1` → **同网段** | 直连无阻碍；Apple TV 接同一路由即可订阅 |
| Docker 服务 | **已开启**，且「开机自动开启」已勾选 | ✅ NAS/容器重启后自动拉起，无需人工干预 |
| Compose 支持 | Docker 应用内有独立 **Compose** 菜单 | ✅ 可直接用 `docker-compose.yml` 部署，不必手填表单 |
| 现有负载 | 3 个项目、2 个容器运行中、8 个镜像，健康状态「0 个异常」 | 我们的服务属增量，不影响现有容器 |
| Docker 存储位置 | 「存储空间 1（Basic）」 | 数据卷挂到该存储空间下即可 |
| **SSH（22 端口）** | **开放** | ✅ **重要**：P4 可经 SSH 用脚本部署与更新，不必在图形界面手点 |
| 端口 8787 / 8080 | 均**空闲**（TCP 探测不通＝未占用） | 订阅服务用 8787 不会与现有容器冲突 |

> 补充两点：
> 1. 桌面装有「节点小宝远程助手」，说明你已有远程访问 NAS 的手段；既然决定只在家看，本期不使用它。
> 2. NAS 管理员密码仅用于本次只读核对，**未写入项目文件、配置或记忆**；浏览器里的登录会话你可自行退出。桌面那份明文密码 txt 建议事后删除或移进密码管理器。

### 2.8 ★ P0 实施期的两处修正（2026-09-18 晚，真跑之后才知道的）

写 P0 代码、真跑一遍之后，前面两节的结论各有一处需要推翻重写。这两条会实质影响后面所有阶段，务必以本节为准。

**修正一：市州台不是「公开源拿不到」，是「找错了源」**

2.2 的"市州台几乎为零"只测了通用聚合源（iptv-api / iptv-org / fanmingming）。换到**运营商口径**的湖南源后，覆盖直接补齐：

| 新接入源（`config/sources.yaml`） | 条目 | 湖南相关覆盖 | 出口主机 |
|---|---|---|---|
| `hn_mobile`（Tvlist-awesome-m3u-m3u8 仓库 `m3u/湖南移动.m3u`） | 192 | **10 省台（含湖南卫视 4K）+ 长沙 4 套 + 13 市州新闻综合 + 市州第二频道 + 县级台**，共 42 条 | `tvgslb.hn.chinamobile.com:8089` |
| `hn_unicom`（同仓库 `m3u/湖南联通.m3u`） | 57 | 省台全家桶 + 长沙新闻/政法/女性 + 怀化/常德/张家界/湘西 | `58.20.64.92:9999` |
| `hn_telecom`（`湖南电信IPTV.m3u`） | 142 | 覆盖同样全，**但全是 `rtp://` 组播** | Apple TV 走 Wi-Fi 播不了，且属已划红线 → `enabled: false` |

合并三个源后的实际产出：`hunan.m3u` 从 **14 频道 / 31 线路** 变成 **39 频道 / 71 线路**，配置里 29 个湖南频道只有「长沙经贸」一个没匹配到。

**P5 的性质因此改变**：不再是"从零找源"，而是**验证这些运营商源在你的电视上到底可不可达 + 补县级台**。工作量从"持续排查"降级为"一次真机实测 + 少量补充"。

**修正二：开发机不是干净的测量点，`--verify` 的结论对它自己都不成立**

P0 一度测出"湖南线路只有 5/31（16%）可用"。查下来根因不在源，也不在代理开关：

- 这台 Windows 上有一个 **`VirtualNet Tunnel`（172.19.0.1）抢走了默认路由**（`0.0.0.0/0`，metric 0），物理网卡 `192.168.1.4` 反而不是出口
- DNS 被接管：`tvgslb.hn.chinamobile.com` 在本机解析成 **`198.18.0.129`（fake-IP 段）**，而阿里 DNS 给出的真实地址是 `36.158.166.3`
- 于是即使代码里显式禁用系统代理、即使目标是真实 IP，请求仍然从隧道口出去并得到 `Empty reply from server` —— 249 条湖南移动/联通线路**全红**，同一个错误码，明显是被隧道掐断，而不是源失效
- 横向对照：iptv-api 源同机实测可用率 69%（236/344），说明隧道只对**国内运营商内网类地址**失效

**结论与对策（已落到代码里）**：

1. 测量结论要分源看，`build --verify` 现在按源打印「可用 n/m」，隧道造成的假阴性一眼能看出来
2. `sources.yaml` 增加 `probe: false`，运营商内网源**跳过本机实测、原样保留**，避免把最想要的湖南台全过滤掉
3. 真正的可用性判定必须来自**看电视那张网**：Apple TV 实播，或 P4 之后由 NAS 探测。在此之前，`report.md` 里明确写「以 Apple TV 实际播放为准」
4. P2 的三层探测，**执行位置默认放在 NAS 容器里**（与电视同网段、出口即家庭宽带），Windows 只用于开发调试 —— 这是对第 6 章的一个部署位置修正

---

## 三、总体架构

```
                    ┌─────────────────────────────────────┐
   上游公开源(多个)  │           本地工具（Python）          │
                    │                                     │
 iptv-api/gd  ─────▶│  ① fetch   抓取 + 缓存 + 镜像回退     │
 fanmingming  ─────▶│      ↓                              │
 iptv-org     ─────▶│  ② parse   m3u / txt 解析为统一结构   │
 手工补源文件 ─────▶│      ↓                              │
                    │  ③ match   频道名归一化 + 别名匹配     │
                    │      ↓      + 同频道多线路合并        │
                    │  ④ check   三层探测 + 并发测速         │
                    │      ↓      + 评分排序 + 黑名单       │
                    │  ⑤ enrich  logo / EPG id / 分组对齐   │
                    │      ↓                              │
                    │  ⑥ output  aptv.m3u / playlist.txt   │
                    └──────────────┬──────────────────────┘
                                   │
                     ┌─────────────┴──────────────┐
                     ▼                            ▼
            ⑦ publish 推送托管              本地文件副本
            （订阅网址 URL）              data/output/*.m3u
                     │
                     ▼
              APTV 订阅并自动刷新
```

**设计原则**：每一步的产物都落盘为中间文件（`data/cache/`、`data/stage/`），任何一步失败都能从上一步的缓存重跑，不必全部重来。这对"上游经常抽风"的场景很重要。

---

## 四、目标频道清单与命名规范

### 4.1 分组结构（APTV 侧边栏顺序）

```
📍 湖南本地       ← 湖南卫视/经视/都市/娱乐/公共/电视剧/电影/国际/教育/爱晚
📍 长沙           ← 长沙新闻综合/政法/女性/经贸 + 长沙县/浏阳/宁乡
📍 市州台         ← 株洲/湘潭/岳阳/衡阳/常德/张家界/益阳/郴州/永州/怀化/娄底/邵阳/湘西
🎬 金鹰系         ← 金鹰卡通/金鹰纪实
📺 央视           ← CCTV-1 … CCTV-17、CCTV-4K/8K、CGTN
🌏 卫视           ← 湖南之外的各省卫视
```

湖南本地频道排在最前，符合"打开就看本地台"的使用习惯。

### 4.2 频道名归一化（匹配引擎的核心）

公开源里同一个频道写法五花八门，必须先归一化再匹配。示例：

| 上游原始名 | 归一化后 | 标准输出名 |
|---|---|---|
| `湖南经视` / `湖南经视HD` / `HNTV-2` / `湖南电视台经视频道` | `hunan jingshi` | 湖南经视 |
| `湖南卫视高清` / `HNTV1` / `湖南卫视 (1080p)` | `hunan weishi` | 湖南卫视 |
| `长沙新闻综合` / `长沙新闻` / `CSTV-1` | `changsha xinwen` | 长沙新闻综合 |

归一化规则：去空格标点 → 去清晰度后缀（HD/FHD/高清/超清/1080p/4K）→ 去括号备注 → 全角转半角 → 拼音/首字母映射表兜底。规则写在 `config/channels.yaml` 的 `aliases` 字段里，**可人工持续补充，不用改代码**。

### 4.3 tvg-id 规范

统一采用 EPG 通用的大写英文 id（如 `HUNANWEISHI`、`HUNANJINGSHI`、`CCTV1`），以便节目单对齐。id 映射表由 `enrich` 模块维护，实施期以实际 EPG 内容为准校准。

---

## 五、源获取策略（三级来源）

| 级别 | 来源 | 用途 | 刷新频率 |
|---|---|---|---|
| **L1 主力** | `iptv-api` 仓库输出（`gd` 分支，路径可配置） | 央视、卫视、湖南省台、金鹰系 | 每日 |
| **L1.5 湖南专项** | `Tvlist-awesome-m3u-m3u8` 仓库的 `湖南移动.m3u` / `湖南联通.m3u`（已接入，见 2.8） | **市州台与长沙台的唯一有效来源**；可达性受运营商网络限制 | 每日 |
| **L2 备用** | `fanmingming/live`、`iptv-org`、其他公开聚合仓库 | L1 抽风时兜底、交叉补充线路 | 每日 |
| **L3 手工** | `config/sources_local.yaml`（你自己维护的补充源文件） | 县级台、L1.5 覆盖不到的零星台 | 人工按需 |

> 上游清单已从代码里挪到 `config/sources.yaml`（含 `enabled` / `probe` / `note` 字段），加源不用改代码。

**镜像回退链**：每个上游 URL 配置多个候选前缀，按顺序尝试直到成功：

```
raw.githubusercontent.com/...      ← 首选
gh-proxy.com/https://raw...        ← 加速镜像
ghproxy.net/https://raw...         ← 备用镜像
```

实测 `live.fanmingming.com` 直连超时、`master` 分支 404，这条回退链是必需的，不是锦上添花。

**同频道多线路**：所有上游命中的同一频道线路**全部保留进入检测阶段**，检测后按分数排序，取前 N 条（默认 N=3）。APTV 会把同名频道合并为多线路，播放失败时可手动/自动切换下一条。

---

## 六、质量检测与排序（本项目技术核心）

### 6.1 三层递进探测

不能只看 HTTP 状态码 —— 实测中失效源往往也返回 200。分三层：

| 层级 | 检测内容 | 超时 | 判定 |
|---|---|---|---|
| **L1 连通** | TCP 连接 + HTTP 响应码 + 首字节时间 | 8 s | 非 2xx → 淘汰 |
| **L2 结构** | 下载播放列表，校验含 `#EXTM3U` 且至少有 1 个有效条目；`.ts` 直链则校验 Content-Type 与字节数 | 10 s | 空列表 / 非法结构 → 淘汰 |
| **L3 内容** | 解析 m3u8 取出**第一个分片地址**，请求其前 64 KB，校验拿到真实视频字节（非 HTML 错误页） | 12 s | 拿不到分片字节 → 淘汰 |

L3 是关键：只有下到真实视频分片，才能确认"这条线路真能播"，而不是一个空壳播放列表。

### 6.2 并发与耗时预算

- `asyncio` + `aiohttp`，默认并发 **80**（可配置）
- 预计 1600 条线路全量三层检测：**3 ～ 6 分钟**
- 支持断点续测：检测结果按 URL 哈希缓存，24 小时内不重复测（可配置）

### 6.3 评分排序

每条存活线路打分，同频道内取前 N：

```
score = 100
      - 首字节延迟惩罚     (延迟 > 1s 起，每 100ms 扣 2 分)
      - L3 分片下载耗时惩罚
      + 清晰度加分         (URL/名称含 1080p|FHD|超清 +8；含 4K +12；含 标清|360p -10)
      + 上游权重加分       (L1 +5 / L2 +3 / L3 +6，手工源可信度高)
      + 历史稳定加分       (连续 7 天存活 +5，见 6.5)
```

### 6.4 ffmpeg（容器内安装，开发机可缺省）

你的 Windows 开发机**未安装 ffmpeg**，但既然部署到飞牛 NAS 的 Docker 里，容器镜像中安装 ffmpeg 只是 Dockerfile 加一行，成本极低，因此按"容器内必装、开发机可选"处理。

装上后可解锁：
- 读取真实**分辨率与码率**，让"优先高清"从"猜 URL 关键词"升级为"实测数值"
- 更可靠的分片解码校验（能识别出"HTTP 200 但视频轨损坏"的假活源）

代码里通过**能力探测自动切换**：检测到 ffmpeg 就启用深度校验，检测不到就退化为纯 HTTP 三层探测。所以 P0～P3 在你的 Windows 上开发调试完全不受影响，上 NAS 后自动获得增强能力。

### 6.5 失败黑名单与历史留存

- `data/blacklist.json`：连续失败 ≥3 次的线路进入黑名单，7 天内不再重复测（省时间），7 天后放出来重试
- `data/history/YYYY-MM-DD.json`：每日结果存档，用于统计线路存活率、生成"历史稳定加分"、以及在出问题时回滚到昨天的可用版本

---

## 七、输出与发布

### 7.1 生成的文件

| 文件 | 用途 |
|---|---|
| `data/output/aptv.m3u` | **主产物**。APTV 订阅用，含 `x-tvg-url`（EPG）、`tvg-id`、`tvg-logo`、`group-title` |
| `data/output/playlist.txt` | 通用 txt 格式，兼容其他播放器/老设备 |
| `data/output/hunan.m3u` | **精简版**，只含湖南本地 + 央视，加载快、命中率高，给 Apple TV 遥控器操作省力 |
| `data/output/report.md` | 本次运行报告：各频道线路数、淘汰数、平均延迟、失败明细 |

输出示例格式：

```
#EXTM3U x-tvg-url="https://.../epg.gz"
#EXTINF:-1 tvg-id="HUNANWEISHI" tvg-name="湖南卫视" tvg-logo="https://.../hunan.png" group-title="📍 湖南本地",湖南卫视
http://<线路1>
#EXTINF:-1 tvg-id="HUNANWEISHI" tvg-name="湖南卫视" tvg-logo="https://.../hunan.png" group-title="📍 湖南本地",湖南卫视
http://<线路2>
```

> ⚠️ **待真机验证**：APTV 对同名多线路的合并行为、以及 `tvg-logo` 的缓存策略，需在 P3 阶段用你的 Apple TV 实测确认，再据实调整输出细节。

### 7.2 订阅网址托管方案

结合你家里已有飞牛 NAS，推荐顺序调整为：

| 优先级 | 方案 | 优点 | 缺点 |
|---|---|---|---|
| **★ 推荐 A** | **飞牛 NAS + Docker，容器内置 HTTP 服务** | 列表**不公开暴露**；局域网加载零延迟；与定时更新同一容器，天然闭环；NAS 常开 | 外出默认访问不到（可加 Tailscale/frp）；需固定 NAS 的 IP 或主机名 |
| 备选 B | GitHub Pages | 免费、全球可达、外出可用 | 播放列表**公开可见**；国内访问 `github.io` 偶有不稳 |
| 备选 C | A + B 双发布 | 在家走 NAS、外出走 Pages | 配置稍复杂；仍需接受列表公开 |

`publish/` 按后端插件式设计，三个方案可并存，切换成本低。

若选 A（推荐），订阅地址形如：

```
http://192.168.1.3:8787/aptv.m3u     ← 主列表（湖南本地 + 央视 + 卫视）
http://192.168.1.3:8787/hunan.m3u    ← 精简版，只有湖南本地（推荐日常用）
http://192.168.1.3:8787/full.m3u     ← 全量版（保留所有存活线路）
http://192.168.1.3:8787/             ← 运行状态报告页 + 补源提交入口
```

需要在路由器上给 NAS 绑定固定 IP（或使用 fnOS 的 mDNS 主机名），避免 DHCP 变动导致 APTV 订阅失效。

### 7.3 定时自动更新

你已选择"定时自动更新"，实现路径：

| 优先级 | 方案 | 说明 |
|---|---|---|
| **★ 推荐** | **NAS 容器内 cron** | 每天 04:00 与 18:00 各跑一次；跑完直接写入容器内 HTTP 服务的静态目录，APTV 下次刷新即生效。**不依赖你的电脑开机** |
| 备选 | GitHub Actions | 云端生成后 push 到 Pages。适合选了方案 B，或想让"生成"与家里网络解耦——但注意云端 IP 抓国内源可能不如家里快，检测结果也可能与你家实际观感不一致 |
| 兜底 | Windows 任务计划程序 | 仅在你暂不想动 NAS 时用；电脑关机就不更新 |

**失败保护**（三种方案通用，避免"自动更新把好源刷成坏源"）：

1. 若本次存活频道数比上次**下降超过 30%**，判定为上游异常，**保留上一版结果不发布**，并写入告警日志
2. 发布采用"先写临时文件 → 校验格式 → 原子替换"，绝不出现半截文件
3. 保留最近 7 个历史版本，可一条命令回滚
4. NAS 方案额外加一条：容器启动与每次请求时自检"上次成功更新时间"，超过 36 小时未更新则在报告页顶部标红提示

---

## 八、实施计划（分阶段，每阶段有可验收产物）

### P0 — 最小可用版（约 0.5 天）｜目标：今晚就能在 Apple TV 上看湖南卫视

- 建项目骨架、`config/channels.yaml` 湖南频道初版
- 直接下载 iptv-api `gd` 输出 → 过滤出「湖南频道 + 央视」→ 生成 `aptv.m3u`
- 手动导入 APTV 验证播放
- **验收**：Apple TV 上能播湖南卫视、湖南经视、CCTV-1，分组和台标正常
- 说明：这一步跳过测速，先让你看到东西，后续阶段再逐步加固

### P1 — 抓取与匹配引擎（约 1 天）

- 多上游抓取（含镜像回退链）、m3u/txt 双格式解析、统一中间结构
- 频道名归一化 + 别名匹配 + 同频道多线路合并 + logo 补齐
- **验收**：输入 3 个上游共 ~1900 条原始线路，正确归类到目标频道，误匹配率 < 2%（人工抽检 50 条）

### P2 — 质量检测与排序（约 1 天）

- 三层探测、80 并发、评分排序、每频道保留 N 条
- 黑名单与历史存档
- **验收**：全量检测 < 8 分钟；随机抽 20 条输出线路，在 Apple TV 上实测**可播率 ≥ 90%**

### P3 — 输出与 EPG（约 0.5 天）

- 生成 `aptv.m3u` / `playlist.txt` / `hunan.m3u` / `report.md`
- EPG 接入与 `tvg-id` 对齐校准
- **验收**：APTV 中频道分组、台标、**节目单**三者均正确显示

### P4 — NAS 部署、定时更新与托管（约 1.5 天）

- 编写 `deploy/Dockerfile` + `docker-compose.yml`，在飞牛 fnOS 的 Docker 中跑起来
- 容器内置 `serve.py`：提供订阅下载、运行状态报告页、（可选）补源提交接口
- 容器内 cron 定时执行全流程；代理通过环境变量注入容器
- 30% 骤降保护、原子发布、7 版本回滚、更新超期告警
- APTV 添加局域网订阅，实测 tvOS「本地网络」权限的实际行为
- **验收**：Apple TV 成功订阅 `http://<NAS_IP>:<端口>/aptv.m3u`；连续 3 天自动运行无人工干预；**关掉 Windows 电脑不影响更新与观看**

### P5 — 湖南地市台补源（2026-09-18 晚已重新定义，见 2.8）

原判断「公开源覆盖不到市州台」已被推翻：`hn_mobile` / `hn_unicom` 两个运营商口径源一次就把 13 市州新闻综合 + 长沙台 + 市州第二频道全部补齐（39 个湖南频道）。P5 因此从"到处找源"改为三件更具体的事：

1. **真机可达性验证（当前最优先）**：这些源很可能只在对应运营商宽带内可播。需要确认你的家庭宽带是哪家，再决定主力源取 `hn_mobile` 还是 `hn_unicom`，并把不可达的源在 `sources.yaml` 里关掉
2. **补县级台**：湖南移动源里还有衡阳县、长沙县、湘潭县等县级频道，需要哪些就加进 `channels.yaml`（坑位已留好，加别名即自动归位）
3. **官方公开流排查**：只对前两步仍覆盖不到的台做，写入 `config/sources_local.yaml`，标注来源与有效期；确无公开流的台在报告里标"暂不可用"，**不硬塞假源**

- **验收**：Apple TV 上实际可播的湖南本地频道 ≥ 25 个，且每个台至少 1 条在你家网络里实测能播的线路

> P0～P4 合计约 4.5 天工作量（P4 因加入 NAS 容器化部署，从 1 天调整为 1.5 天）。P5 已不再是"可能整块做不出来"的高风险项，剩余风险收敛为**运营商网络归属**这一个具体问题，一次真机实测即可判定。
>
> **好消息**：代理可用让排查官方公开流的成功率明显提高。部分市州台的官方直播流托管在海外 CDN 或需要较通畅的国际出口才能稳定访问，之前这类源基本可以直接判死，现在值得逐个试。

---

## 九、目录结构

```
iptv电视源/
├─ config/
│  ├─ channels.yaml        # 目标频道：标准名、别名、分组、优先级、tvg-id
│  ├─ sources.yaml         # 上游公开源：URL、镜像链、权重、启用开关
│  ├─ sources_local.yaml   # ★ 手工补充源（湖南地市台主战场）
│  └─ settings.yaml        # 并发数、超时、保留线路数、检测深度、发布配置
├─ src/
│  ├─ fetch/               # ① 抓取：http_client.py / mirror.py / cache.py
│  ├─ parse/               # ② 解析：m3u_parser.py / txt_parser.py / model.py
│  ├─ match/               # ③ 匹配：normalize.py / matcher.py / dedup.py
│  ├─ check/               # ④ 检测：prober.py / scorer.py / blacklist.py
│  ├─ enrich/              # ⑤ 增强：epg.py / logo.py / group.py
│  ├─ output/              # ⑥ 输出：writer.py / report.py
│  ├─ publish/             # ⑦ 发布：nas_local.py（默认）/ github_pages.py
│  └─ cli.py               # 命令行入口
├─ scripts/
│  ├─ run_update.py        # 定时任务入口（全流程）
│  └─ rollback.py          # 回滚到指定历史版本
├─ deploy/                 # ★ 飞牛 NAS 部署（推荐方案 A）
│  ├─ Dockerfile           # Python 3.12 运行环境 + ffmpeg（可选）
│  ├─ docker-compose.yml   # 端口/数据卷/代理环境变量
│  ├─ serve.py             # 轻量 HTTP 服务：订阅下载 + 状态报告页 + 补源提交
│  ├─ cron.sh              # 容器内定时任务入口
│  └─ README-飞牛部署.md    # fnOS 图形化 Docker 里的逐步操作说明
├─ data/
│  ├─ cache/               # 上游抓取缓存
│  ├─ stage/               # 各阶段中间产物
│  ├─ output/              # ★ 最终 m3u / txt / 报告
│  ├─ history/             # 每日结果存档
│  └─ blacklist.json
├─ tests/                  # 解析、归一化、评分的单元测试
├─ .github/workflows/      # 定时任务（若选 GitHub Actions）
├─ 调研样本/                # 本次调研抓取的真实样本（已存在）
└─ 计划书.md                # 本文档
```

## 十、技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 语言 | **Python 3.12** | 本机已装；IPTV 生态与 iptv-api 上游均为 Python，解析/复用成本最低 |
| 并发 | `asyncio` + `aiohttp` | 1600 条线路的 IO 密集检测，异步比线程池快数倍 |
| 配置 | YAML | 频道别名、上游源需要**你手工频繁编辑**，YAML 比 JSON 友好且支持注释 |
| 依赖 | `aiohttp`、`pyyaml`、`rich`（进度条） | 依赖越少越好，NAS 上装得快、镜像小 |
| 部署 | **Docker（飞牛 fnOS 图形化管理）** | NAS 常开；容器化后环境与你的 Windows 开发机解耦，升级/回滚都是换镜像 |
| HTTP 服务 | 复用 `aiohttp` 起一个轻量服务 | 不额外引入 Flask/FastAPI，少一个依赖；只提供订阅下载、报告页、补源提交三个接口 |
| ffmpeg | **容器内安装**（`apt-get install ffmpeg`） | 开发机上装麻烦，容器里只是一行；用于实测分辨率/码率，让"优先高清"从猜关键词升级为读数值 |
| 代理 | 通过环境变量注入容器，`aiohttp` 统一走代理连接器 | 抓取海外源与 GitHub；国内源可配置直连以免绕路变慢 |
| 日志 | 标准库 `logging` + 每次运行落盘 | 定时任务无人值守，出问题要能回溯 |

---

## 十一、合规声明（请务必阅读）

1. 本项目**仅用于个人学习与家庭自用观看**，不对外分发、不商用、不盈利
2. 所有源均来自**公开可访问的网络资源**，本工具不破解任何加密、不绕过任何登录鉴权、不盗链需授权的私有流
3. 若某频道的权利人或运营方提出异议，将从 `sources_local.yaml` 中**立即移除**该源
4. 已明确排除运营商组播扫描 —— 那类行为可能违反宽带服务条款
5. 订阅托管优先选择**家中飞牛 NAS 的局域网方案（7.2 节方案 A）**，播放列表不出家门，避免被他人滥用；若确实需要公网托管（方案 B/C），应加访问口令或 IP 白名单，并定期更换地址
6. NAS 上的补源提交网页**只监听局域网**，不映射到公网，避免变成公开的源提交入口

---

## 十二、决策点（2026-09-18 已全部确认）

你已逐项拍板，下表为**最终执行口径**：

| # | 决策点 | ✅ 已确认执行 | 未采纳的备选 |
|---|---|---|---|
| 1 | **部署形态** | **飞牛 NAS + Docker**：更新与托管都在家里，私密、常开、加载快 | GitHub Actions 云端 + GitHub Pages ／ 先在 Windows 本地跑通再上 NAS |
| 2 | **外出时是否也要能看** | **只在家看**，不做穿透（本期最简，P4 即收尾） | 加 Tailscale ／ frp 内网穿透 |
| 3 | **P5 补源方式** | **在 NAS 上做局域网小网页**，手机粘贴 URL 即可入库 | 手工编辑 `sources_local.yaml` |
| 4 | **是否用 ffmpeg** | **容器里直接装**，换取真实码率/分辨率排序 | 纯 HTTP 探测 |
| 5 | 每频道保留几条线路 | 3 条（默认值，配置可调） | 5 条（更抗失效） |
| 6 | 要不要精简版 `hunan.m3u` | 要（Apple TV 遥控器操作更省力） | 只要全量版 |
| 7 | **开工顺序** | **先在 Windows 跑通 P0**，你在 Apple TV 上确认能看湖南台后，再推进 P1～P4 上 NAS | 直接上 NAS 一次搭完 ／ 代码全写完再部署 |

> 原第 1、2 项（托管位置 / 定时任务位置）已合并为"部署形态"一项；ffmpeg 因走 Docker 容器部署，安装成本降为 Dockerfile 一行，故直接采纳。

**原先待你确认的 3 个 NAS 信息已全部实测拿到**（见 2.7）：NAS 地址 `192.168.1.3`、Docker 已开启且支持 Compose、开发机与 NAS 同网段。

**仅剩 1 项待验证，且不阻塞 P0**：Apple TV 是否也接在 `192.168.1.x` 这台路由器下（若走了访客网络或二级路由则无法直连）——P4 配置订阅时一并验证。

另外一条建议：在路由器上给 `192.168.1.3` **绑定静态 DHCP 租约**，避免 NAS 日后换 IP 导致 APTV 订阅地址失效。

---

## 十三、风险清单

| 风险 | 可能性 | 影响 | 应对 |
|---|---|---|---|
| ~~湖南 14 市州台找不到公开可用源~~（已消解，见 2.8） | ~~高~~ → **低** | 覆盖已补齐 39 个湖南频道 | 新风险改为：**运营商内网源在你家宽带不可达** → 一次 Apple TV 真机实测判定，`hn_mobile`/`hn_unicom` 二选一保留 |
| 开发机出口被 `VirtualNet Tunnel` 接管，本机实测对国内源系统性假阴性 | **高**（影响判断，不影响产物） | 误把可用源判死 | 已对策：分源打印可用率、`probe: false` 跳过内网源、探测执行位置移到 NAS（P2/P4） |
| 上游仓库改分支/路径导致 404（实测 master 已 404） | 高 | 更新失败 | `sources.yaml` 配置化 + 多候选路径自动探测 + L2 备用源 |
| GitHub/镜像在国内访问不稳（实测 fanmingming 域名直连超时） | **中**（有代理后下调） | 抓取失败 | 容器内走代理 + 三级镜像回退链兜底 + 本地缓存 |
| 带签名参数的源过期（实测芒果系 URL 含时效 token） | 高 | 线路快速失效 | 每日检测自动剔除；此类源不进"历史稳定加分" |
| m3u8 返回 200 但实际不可播 | 中 | 用户点开是黑屏 | L3 分片层探测（第 6.1 节） |
| APTV 对 tvg-id / 多线路的处理与预期不符 | 中 | 节目单空白、线路不合并 | P3 真机实测后调整输出格式 |
| 自动更新把可用列表刷成不可用 | 中 | 电视看不了 | 30% 骤降保护 + 原子发布 + 7 版本回滚 |
| **NAS 局域网 IP 因 DHCP 变动** | 中 | APTV 订阅地址失效 | 路由器绑定静态 IP，或改用 fnOS 的 mDNS 主机名 |
| **tvOS「本地网络」权限拦截局域网订阅** | 中 | Apple TV 拉不到列表 | P4 真机实测确认；备选走 Tailscale 虚拟网卡，或改由 GitHub Pages 提供 HTTPS 地址 |
| **容器内代理未正确注入** | 中 | 抓源变慢/失败，线路质量下降 | `docker-compose.yml` 显式注入代理环境变量；启动时做一次连通性自检并打印结果 |
| **Apple TV 与 NAS 不在同一网段** | 低～中 | 无法直连订阅 | 部署前先确认网络拓扑；必要时用 Tailscale 打通两端 |
| NAS 重启 / 容器异常导致长期未更新 | 低 | 看的是旧列表（通常仍可播） | 容器 `restart: unless-stopped` + 超期告警 + 报告页标红 |

---

## 十四、下一步（2026-09-18 晚更新，P0 已产出）

**已完成**：P0 代码与配置全部跑通，`data/output/` 下有 `aptv.m3u`（90 频道）、`hunan.m3u`（39 频道）、`report.md`。命令：

```
.venv/Scripts/python.exe -m src.cli build            # 用缓存离线生成
.venv/Scripts/python.exe -m src.cli build --fresh    # 联网抓最新上游
.venv/Scripts/python.exe -m src.cli build --verify   # 实测剔除失效（湖南内网源自动跳过）
```

**卡在你这边的唯一一步 —— 真机验收**：因为 2.8 的隧道问题，湖南台能不能播只能由 Apple TV 判定。

1. 起局域网服务：`.venv/Scripts/python.exe scripts/serve_lan.py`，APTV 里订阅 `http://192.168.1.4:8787/hunan.m3u`
2. 依次点开：湖南卫视、湖南经视、长沙新闻综合、株洲新闻综合（市州台代表）、CCTV-1
3. 把结果告诉我，两种分支：
   - **移动源能播** → 说明你家是湖南移动宽带（或该门户公网可达），主力源定为 `hn_mobile`，P5 基本收工，直接进 P1/P2
   - **移动源全不播、联通源能播** → 主力换成 `hn_unicom`，并在 P5 里为移动台补公网源
   - **两者都不播** → 这批源是运营商内网限定，P5 回到"排查官方公开流"的原始路线，我按市州逐个排查

> 顺带确认一件事：家庭宽带是**移动 / 联通 / 电信**哪一家？这直接决定主力源选谁，比任何实测都快。

**之后的顺序**：P1 匹配引擎加固（含 `normalize` 对「CCTV-5+」这类带符号名的处理）→ P2 三层探测（**执行位置放 NAS**）→ P3 EPG 对齐 → P4 NAS 部署。
